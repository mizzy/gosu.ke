{
  "name": "gosu_ke",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --live-reload --test-scheduled",
    "deploy": "wrangler deploy --minify"
  },
  "dependencies": {
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.87.0"
  }
}
